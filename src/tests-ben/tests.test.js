import {findAverage, findUser} from '../util'




describe('tests findAverage', () => {
  it('should be a function', () => {
    expect(typof findAverage).toBe('function')
  })

  it('should return a integer', () => {
    expect(typeof findAverage()).toBe('integer')
  })

  it('should take ')
})
